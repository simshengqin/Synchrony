import {Group} from './group';
import {Attachment} from './attachment';

export interface Instructor {
  docId?: string;
  firstName?: string;
  lastName?: string;
  school?: string;
  group?: string;
}
